# 💻 Ejercicio: Suma de Pares

Implementa una función que reciba un arreglo de enteros y devuelva la suma de los números pares.

### 📥 Entrada

- Un `std::vector<int>` con números enteros (positivos, negativos o cero).

### 📤 Salida

- Un número entero que representa la suma de todos los elementos pares.

### 🧪 Ejemplos

```cpp
sum_even({1, 2, 3, 4, 5, 6}) => 12
sum_even({1, 3, 5})          => 0
sum_even({2, -2, 4})         => 4
